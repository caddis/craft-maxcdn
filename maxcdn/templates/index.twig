{% extends 'maxcdn/layouts/cp' %}

{% set content %}
	<table class="data fullwidth">
		<thead>
			<tr>
				<th scope="col">{{ 'Filename'|t }}</th>
				<th scope="col">{{ 'Cache Date'|t }}</th>
				<th scope="col">{{ 'Hits'|t }}</th>
				<th scope="col">{{ 'Size'|t }}</th>
			</tr>
		</thead>
		<tbody>
			{% for file in files %}
				<tr>
					<td>{{ file.uri }}</td>
					<td>{{ file.timestamp|date('M d, Y') }}</td>
					<td>{{ file.hit }}</td>
					<td>{{ file.size }}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endset %}